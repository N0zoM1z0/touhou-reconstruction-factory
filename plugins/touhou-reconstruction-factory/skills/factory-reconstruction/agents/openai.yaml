interface:
  display_name: "Factory Reconstruction"
  short_description: "Reconstruct autonomously with Git checkpoints"
  default_prompt: "Use $factory-reconstruction to advance th105 in one bounded Web conversation, checkpoint it, and preserve the next handoff."
