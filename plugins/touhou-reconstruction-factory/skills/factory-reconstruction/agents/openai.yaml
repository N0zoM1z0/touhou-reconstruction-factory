interface:
  display_name: "Factory Reconstruction"
  short_description: "Reconstruct autonomously with Git checkpoints"
  default_prompt: "Use $factory-reconstruction to continue one bounded th105 task in its live repository and create reviewable local checkpoints."
